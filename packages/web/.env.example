# GitHub OAuth (create at https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# NextAuth (generate with: openssl rand -base64 32)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# Control Plane (K8s service URL or ingress hostname)
CONTROL_PLANE_URL=https://control-plane.YOUR-DOMAIN.com
NEXT_PUBLIC_WS_URL=wss://control-plane.YOUR-DOMAIN.com

# Control Plane Authentication (must match control plane's INTERNAL_CALLBACK_SECRET)
# Generate with: openssl rand -base64 32
INTERNAL_CALLBACK_SECRET=

# Access Control (comma-separated, leave empty to allow all GitHub users)
ALLOWED_EMAIL_DOMAINS=
ALLOWED_USERS=
